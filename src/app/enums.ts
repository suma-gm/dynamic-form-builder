export enum Role {
  Admin = 'admin',
  User = 'user',
}

export enum InputType {
  Checkbox = 'checkbox',
  Date = 'date',
  Dropdown = 'dropdown',
  Radio = 'radio',
  Text = 'text',
  TextArea = 'textarea',
}
